name: Gatsby Publish

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ayokevin/gatsby-gh-pages-action@v2
        with:
          access-token: ${{ secrets.GATSBY_UNIR_TOKEN_ACCESS }}
          deploy-branch: gh-pages
          gatsby-args: --prefix-paths